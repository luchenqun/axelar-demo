# Axelar Local Cross-Chain Demo

本项目演示如何使用 Axelar 在本地环境中实现两个 EVM 节点之间的跨链 ERC20 代币转账。

## 📚 文档

详细的使用指南请参考：**[跨链转账指南](./docs/跨链转账指南.md)**

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

**注意：** 推荐使用 Node.js v18（支持 v16 或 v18）

### 2. 启动本地节点

```bash
# 启动两个 Reth 节点
npm start

# 停止节点
npm stop

# 重启节点
npm run restart

# 重置节点（清除数据）
npm run reset
```

### 3. 运行跨链转账示例

```bash
# 使用 TypeScript 直接运行
npm run demo

# 或者先编译再运行
npm run build
npm run prod
```

## 🏗️ 项目结构

```
axelar-demo/
├── src/                    # TypeScript 源代码
│   ├── index.ts           # 主入口文件
│   └── evm.ts             # EVM 跨链示例
├── scripts/               # 节点管理脚本
│   ├── start.sh          # 启动节点
│   ├── stop.sh           # 停止节点
│   ├── restart.sh        # 重启节点
│   └── reset.sh          # 重置节点
├── chaindata/             # 区块链数据
│   ├── chain-a-genesis.json
│   ├── chain-b-genesis.json
│   └── reth_config
├── docs/                  # 文档目录
│   └── 跨链转账指南.md   # 详细使用指南
├── docker-compose.yml     # Docker 配置
├── tsconfig.json          # TypeScript 配置
└── package.json           # 项目配置
```

## 🔧 可用命令

### 节点管理
- `npm start` - 启动两个 Reth 节点
- `npm stop` - 停止节点
- `npm run restart` - 重启节点
- `npm run reset` - 重置节点（清除所有数据）

### 开发
- `npm run build` - 编译 TypeScript
- `npm run dev` - 开发模式运行
- `npm run demo` - 运行跨链演示
- `npm run prod` - 生产模式运行

## 📡 节点信息

- **Chain A**: http://localhost:8545 (chainId: 9)
- **Chain B**: http://localhost:7545 (chainId: 10)

## 💡 核心功能

✅ 两个本地 EVM 节点（基于 Reth）
✅ Axelar Gateway 合约部署
✅ ERC20 代币跨链转账
✅ 自动中继器（Relayer）
✅ TypeScript 支持
✅ Docker 容器化部署

## 📖 学习资源

- [详细跨链指南](./docs/跨链转账指南.md) - 完整的步骤说明和代码示例
- [Axelar 官方文档](https://docs.axelar.dev/)
- [Reth 文档](https://paradigmxyz.github.io/reth/)

## ⚠️ 注意事项

1. **Node.js 版本**：推荐使用 v18（支持 v16 或 v18）
2. **跨链费用**：本地环境每次跨链扣除 1 个代币单位
3. **必须调用 relay()**：每次跨链操作后需要调用 `relay()` 函数

## 🐛 故障排查

遇到问题？查看 [故障排查指南](./docs/跨链转账指南.md#故障排查)

常见问题：
- gas limit too high → 检查 gasLimit 设置
- 节点不出块 → 确认 docker-compose.yml 配置
- 账户余额不足 → 检查创世块配置

## 📄 许可证

MIT

---

**项目版本：** 1.0.0
**最后更新：** 2025-11-25
