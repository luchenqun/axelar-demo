# Axelar 本地跨链转账指南

## 📋 目录

- [架构概述](#架构概述)
- [前置条件](#前置条件)
- [快速开始](#快速开始)
- [详细步骤](#详细步骤)
- [代码示例](#代码示例)
- [关键概念](#关键概念)
- [故障排查](#故障排查)
- [API 参考](#api-参考)

## 🏗️ 架构概述

### 系统架构

```
┌─────────────────┐         ┌─────────────────┐
│   Chain A       │         │   Chain B       │
│  (Reth 节点)    │         │  (Reth 节点)    │
│  localhost:8545 │         │  localhost:7545 │
└────────┬────────┘         └────────┬────────┘
         │                           │
         │                           │
         └──────────┐   ┐───────────┘
                    │   │
            ┌───────▼───▼────────┐
            │  Axelar Network    │
            │  (本地开发环境)     │
            │  - Gateway 合约     │
            │  - Relayer 中继器   │
            │  - ITS 服务         │
            └────────────────────┘
```

### 核心组件

1. **两个 EVM 节点**
   - Chain A: http://localhost:8545 (chainId: 9)
   - Chain B: http://localhost:7545 (chainId: 10)

2. **Axelar 组件**
   - Gateway 合约：处理跨链消息和代币转账
   - Gas Receiver：收取跨链费用
   - InterchainTokenService (ITS)：管理跨链代币
   - Relayer：中继跨链交易

## ✅ 前置条件

### 1. 环境要求

- Node.js: v16 或 v18 (推荐 v18)
- Docker: 用于运行 Reth 节点
- Git: 版本控制

### 2. 已安装的依赖

```bash
npm install
```

主要依赖：
- `@axelar-network/axelar-local-dev`: Axelar 本地开发工具
- `ethers`: 以太坊交互库
- `typescript`: TypeScript 支持

### 3. 启动本地节点

```bash
# 启动两个 Reth 节点
npm start

# 验证节点运行
curl -X POST http://localhost:8545 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'

curl -X POST http://localhost:7545 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'
```

## 🚀 快速开始

### 方式一：使用现成示例

```bash
# 运行跨链转账示例
npm run demo
```

### 方式二：创建自定义跨链脚本

参见下面的[详细步骤](#详细步骤)。

## 📝 详细步骤

### 步骤 1：连接到现有的 EVM 节点

```typescript
import { setupNetwork, relay } from '@axelar-network/axelar-local-dev';
import { ethers } from 'ethers';

// 连接到你的两个 EVM 节点
const RPC_URL_A = 'http://localhost:8545';
const RPC_URL_B = 'http://localhost:7545';

// 使用有余额的私钥（需要在创世块中预分配）
const PRIVATE_KEY = '0xf78a036930ce63791ea6ea20072986d8c3f16a6811f6a2583b0787c45086f769';
```

### 步骤 2：初始化 Axelar 网络

```typescript
// 为 Chain A 设置 Axelar Gateway 和相关合约
const chainA = await setupNetwork(RPC_URL_A, {
  name: 'ChainA',
  ownerKey: new ethers.Wallet(PRIVATE_KEY, new ethers.providers.JsonRpcProvider(RPC_URL_A)),
});

// 为 Chain B 设置 Axelar Gateway 和相关合约
const chainB = await setupNetwork(RPC_URL_B, {
  name: 'ChainB',
  ownerKey: new ethers.Wallet(PRIVATE_KEY, new ethers.providers.JsonRpcProvider(RPC_URL_B)),
});
```

**`setupNetwork` 做了什么：**
- 部署 ConstAddressDeployer
- 部署 Create3Deployer
- 部署 Axelar Gateway
- 部署 Gas Receiver
- 部署 InterchainTokenService (ITS)

### 步骤 3：部署 ERC20 代币

```typescript
// 在 Chain A 上部署 USDC（初始供应量 100,000）
const tokenA = await chainA.deployToken(
  'USDC',           // 代币符号
  'aUSDC',          // 代币名称
  6,                // 小数位数
  BigInt(100000 * 1e6)  // 初始供应量
);

// 在 Chain B 上部署 USDC（初始供应量 0，因为代币会从 Chain A 跨链过来）
const tokenB = await chainB.deployToken(
  'USDC',
  'aUSDC',
  6,
  BigInt(0)
);
```

### 步骤 4：准备钱包

```typescript
const providerA = new ethers.providers.JsonRpcProvider(RPC_URL_A);
const providerB = new ethers.providers.JsonRpcProvider(RPC_URL_B);

const walletA = new ethers.Wallet(PRIVATE_KEY, providerA);
const walletB = new ethers.Wallet(PRIVATE_KEY, providerB);
```

### 步骤 5：执行跨链转账

```typescript
// 转账金额（100 USDC）
const amount = 100 * 1e6;

// 1. 授权 Gateway 合约扣款
const approveTx = await tokenA
  .connect(walletA)
  .approve(chainA.gateway.address, amount);
await approveTx.wait();

// 2. 调用 Gateway.sendToken 发起跨链转账
const sendTx = await chainA.gateway
  .connect(walletA)
  .sendToken(
    chainB.name,           // 目标链名称
    walletB.address,       // 接收地址
    'aUSDC',               // 代币符号
    amount                 // 转账数量
  );
await sendTx.wait();

// 3. 运行 Relayer 中继跨链交易
await relay();
```

### 步骤 6：验证结果

```typescript
const balanceA = await tokenA.balanceOf(walletA.address);
const balanceB = await tokenB.balanceOf(walletB.address);

console.log(`Chain A 余额: ${ethers.utils.formatUnits(balanceA, 6)} aUSDC`);
console.log(`Chain B 余额: ${ethers.utils.formatUnits(balanceB, 6)} aUSDC`);
```

## 💻 代码示例

### 完整示例：Chain A → Chain B

创建文件 `src/cross-chain-transfer.ts`：

```typescript
import { setupNetwork, relay } from '@axelar-network/axelar-local-dev';
import { ethers } from 'ethers';

async function main(): Promise<void> {
  console.log('🚀 开始跨链转账流程...\n');

  // 配置
  const RPC_URL_A = 'http://localhost:8545';
  const RPC_URL_B = 'http://localhost:7545';
  const PRIVATE_KEY = '0xf78a036930ce63791ea6ea20072986d8c3f16a6811f6a2583b0787c45086f769';

  // 连接节点
  const providerA = new ethers.providers.JsonRpcProvider(RPC_URL_A);
  const providerB = new ethers.providers.JsonRpcProvider(RPC_URL_B);
  const walletA = new ethers.Wallet(PRIVATE_KEY, providerA);
  const walletB = new ethers.Wallet(PRIVATE_KEY, providerB);

  // 初始化 Axelar 网络
  console.log('📡 初始化 Chain A...');
  const chainA = await setupNetwork(RPC_URL_A, {
    name: 'ChainA',
    ownerKey: walletA,
  });
  console.log(`   ✅ Gateway: ${chainA.gateway.address}\n`);

  console.log('📡 初始化 Chain B...');
  const chainB = await setupNetwork(RPC_URL_B, {
    name: 'ChainB',
    ownerKey: walletB,
  });
  console.log(`   ✅ Gateway: ${chainB.gateway.address}\n`);

  // 部署代币
  console.log('🪙 部署 USDC 代币...');
  const tokenA = await chainA.deployToken('USDC', 'aUSDC', 6, BigInt(100000 * 1e6));
  const tokenB = await chainB.deployToken('USDC', 'aUSDC', 6, BigInt(0));
  console.log(`   Chain A: ${tokenA.address}`);
  console.log(`   Chain B: ${tokenB.address}\n`);

  // 转账
  const amount = 100 * 1e6; // 100 USDC
  console.log(`💸 准备转账 ${amount / 1e6} USDC...`);

  // 授权
  console.log('   1️⃣  授权 Gateway...');
  const approveTx = await tokenA.connect(walletA).approve(chainA.gateway.address, amount);
  await approveTx.wait();

  // 发送跨链交易
  console.log('   2️⃣  发送跨链交易...');
  const sendTx = await chainA.gateway.connect(walletA).sendToken(
    chainB.name,
    walletB.address,
    'aUSDC',
    amount
  );
  await sendTx.wait();

  // 中继
  console.log('   3️⃣  运行中继器...');
  await relay();

  // 验证
  console.log('\n📊 最终余额:');
  const balA = await tokenA.balanceOf(walletA.address);
  const balB = await tokenB.balanceOf(walletB.address);
  console.log(`   Chain A: ${ethers.utils.formatUnits(balA, 6)} aUSDC`);
  console.log(`   Chain B: ${ethers.utils.formatUnits(balB, 6)} aUSDC`);

  console.log('\n✅ 跨链转账完成！');
}

main().catch(console.error);
```

运行：

```bash
ts-node src/cross-chain-transfer.ts
```

### 示例：双向转账

```typescript
// Chain A → Chain B
await chainA.gateway.connect(walletA).sendToken(
  chainB.name,
  walletB.address,
  'aUSDC',
  50 * 1e6
);
await relay();

// Chain B → Chain A
await chainB.gateway.connect(walletB).sendToken(
  chainA.name,
  walletA.address,
  'aUSDC',
  25 * 1e6
);
await relay();
```

## 🔑 关键概念

### 1. Gateway 合约

Axelar Gateway 是跨链通信的核心合约，主要功能：

```solidity
// 发送代币到其他链
function sendToken(
    string calldata destinationChain,
    string calldata destinationAddress,
    string calldata symbol,
    uint256 amount
) external;

// 执行跨链命令（由 Relayer 调用）
function execute(bytes calldata input) external;
```

### 2. InterchainTokenService (ITS)

ITS 管理跨链代币的注册和铸造：

- 在源链锁定或销毁代币
- 在目标链铸造或释放代币
- 保证代币总量恒定

### 3. Relayer 中继器

Relayer 监听源链事件，在目标链执行相应操作：

```typescript
await relay(); // 自动处理所有待处理的跨链交易
```

### 4. 跨链费用

本地开发环境中，每次跨链转账会收取 **固定 1 个代币单位**的费用：

```javascript
// 在 @axelar-network/axelar-local-dev 中
const getFee = () => {
    return 1e6;  // 1 USDC (6位小数)
};
```

因此：
- 转账 100 USDC → 实际到账 99 USDC
- 转账 50 USDC → 实际到账 49 USDC

## 🔧 故障排查

### 问题 1：gas limit too high

**错误信息：**
```
Error: processing response error (body="{\"jsonrpc\":\"2.0\",\"id\":365,\"error\":{\"code\":-32003,\"message\":\"gas limit too high\"}}"
```

**原因：** 交易的 gasLimit 超过了 Reth 的限制（通常是 30M）

**解决方法：**
1. 检查 `node_modules/@axelar-network/axelar-local-dev/dist/Network.js`
2. 确保所有 `gasLimit` 设置不超过 28M：
   ```javascript
   { gasLimit: BigInt(28000000) }  // ✅ 正确
   { gasLimit: BigInt(50000000) }  // ❌ 太高
   ```

### 问题 2：节点不出块

**症状：** `eth_blockNumber` 返回的区块号不增长

**解决方法：**
1. 检查 docker-compose.yml 中是否有 `--dev.block-time 1000ms` 参数
2. 重启节点：
   ```bash
   npm run reset
   ```

### 问题 3：账户余额不足

**错误信息：**
```
Error: insufficient funds for gas * price + value
```

**解决方法：**
1. 确保私钥对应的地址在创世块中有预分配余额
2. 检查 `chaindata/chain-a-genesis.json` 和 `chaindata/chain-b-genesis.json`
3. 验证地址：
   ```bash
   node -e "const ethers = require('ethers'); console.log(new ethers.Wallet('YOUR_PRIVATE_KEY').address);"
   ```

### 问题 4：合约部署失败

**错误信息：**
```
Error: cannot estimate gas; transaction may fail
```

**可能原因：**
1. 节点未正常运行
2. gasLimit 设置不正确
3. 账户余额不足

**解决方法：**
```bash
# 1. 重启节点
npm run reset

# 2. 验证节点状态
curl -X POST http://localhost:8545 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"eth_blockNumber","params":[],"id":1}'

# 3. 检查余额
curl -X POST http://localhost:8545 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"eth_getBalance","params":["YOUR_ADDRESS","latest"],"id":1}'
```

### 问题 5：跨链交易未到账

**检查步骤：**

1. 确认源链交易已确认：
   ```typescript
   const receipt = await sendTx.wait();
   console.log('Transaction hash:', receipt.transactionHash);
   ```

2. 确认调用了 relay()：
   ```typescript
   await relay();  // 必须调用！
   ```

3. 检查目标链事件日志

## 📚 API 参考

### setupNetwork()

初始化 Axelar 网络环境。

```typescript
setupNetwork(
  rpcUrl: string,
  options: {
    name: string;           // 链名称
    ownerKey: Wallet;       // 所有者钱包
  }
): Promise<Network>
```

**返回值：**
```typescript
interface Network {
  name: string;
  gateway: Contract;              // Axelar Gateway 合约
  gasService: Contract;           // Gas Service 合约
  interchainTokenService: Contract; // ITS 合约
  deployToken: (symbol, name, decimals, supply) => Promise<Contract>;
  giveToken: (address, symbol, amount) => Promise<void>;
}
```

### relay()

处理所有待处理的跨链交易。

```typescript
relay(): Promise<void>
```

**注意：** 每次跨链操作后都必须调用 `relay()`。

### Gateway.sendToken()

发起跨链代币转账。

```typescript
gateway.sendToken(
  destinationChain: string,    // 目标链名称（如 'ChainB'）
  destinationAddress: string,  // 目标地址
  symbol: string,              // 代币符号（如 'aUSDC'）
  amount: BigNumberish         // 转账数量（最小单位）
): Promise<TransactionResponse>
```

### deployToken()

在链上部署 ERC20 代币。

```typescript
network.deployToken(
  symbol: string,      // 代币符号
  name: string,        // 代币名称
  decimals: number,    // 小数位数
  supply: bigint       // 初始供应量（最小单位）
): Promise<Contract>
```

## 📖 相关资源

- [Axelar 官方文档](https://docs.axelar.dev/)
- [axelar-local-dev GitHub](https://github.com/axelarnetwork/axelar-local-dev)
- [Ethers.js 文档](https://docs.ethers.org/v5/)
- [Reth 文档](https://paradigmxyz.github.io/reth/)

## 🎯 最佳实践

1. **总是先检查节点状态**
   ```bash
   npm start  # 启动节点
   # 等待几秒让节点完全启动
   ```

2. **使用 TypeScript**
   - 类型安全
   - 更好的 IDE 支持
   - 减少运行时错误

3. **合理设置 gasLimit**
   - 合约部署：10-28M
   - 普通转账：100K-500K
   - 复杂操作：1-5M

4. **记得调用 relay()**
   ```typescript
   await sendTx.wait();
   await relay();  // 必须！
   ```

5. **处理跨链费用**
   - 本地环境固定扣除 1 个代币单位
   - 转账时考虑费用：转 101 个才能收到 100 个

6. **错误处理**
   ```typescript
   try {
     await relay();
   } catch (error) {
     console.error('Relay failed:', error);
     // 重试逻辑
   }
   ```

## 🚀 下一步

- 尝试多链互通（3个或更多链）
- 实现自定义跨链消息
- 集成前端界面
- 部署到测试网

---

**版本：** 1.0.0
**最后更新：** 2025-11-25
